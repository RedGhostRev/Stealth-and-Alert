package net.rev.stealthandalert.attribute;

import net.minecraft.world.entity.ai.attributes.RangedAttribute;

public class AssassinationDamageAttribute extends RangedAttribute {
    public AssassinationDamageAttribute(String descriptionId, double defaultValue, double min, double max) {
        super(descriptionId, defaultValue, min, max);
    }
}
