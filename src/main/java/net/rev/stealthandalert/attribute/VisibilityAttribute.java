package net.rev.stealthandalert.attribute;

import net.minecraft.world.entity.ai.attributes.RangedAttribute;

public class VisibilityAttribute extends RangedAttribute {
    public VisibilityAttribute(String descriptionId, double defaultValue, double min, double max) {
        super(descriptionId, defaultValue, min, max);
    }
}
