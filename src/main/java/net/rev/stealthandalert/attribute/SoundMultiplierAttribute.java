package net.rev.stealthandalert.attribute;

import net.minecraft.world.entity.ai.attributes.RangedAttribute;

public class SoundMultiplierAttribute extends RangedAttribute {
    public SoundMultiplierAttribute(String descriptionId, double defaultValue, double min, double max) {
        super(descriptionId, defaultValue, min, max);
    }
}
